package gameWorld;

public class GreatForest extends NodeT1 {

	public GreatForest(int scene) {
		super(scene);

		setBiome("forest");
		setScene(scene);
		setMainDescription("The forest is tall, the trees are silent, no beasts inside, something bad has happened");
		setDistantDescription("Great trees hide all kinds of paths and secrets in the distance");

	}

}
