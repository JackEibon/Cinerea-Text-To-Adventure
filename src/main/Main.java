package main;

import views.MyPanel;

public class Main {

	public static void main(String[] args) {
		MyWindow window = new MyWindow(); //Instanciamos la clase window que funcionara como la ventana donde trabajaremos
		MyPanel panel = new MyPanel(); //Instanciamos la clase panel que funcionara como un panel dentro de las ventanas
		
	}

}
